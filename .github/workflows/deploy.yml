name: Deploy ESS2 Frontend
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - uat
          - prod
jobs:
  deploy:
    name: Deploy to ${{ github.event.inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Set deploy variables
        id: vars
        run: |
          if [ "${{ github.event.inputs.environment }}" = "prod" ]; then
            echo "deploy_path=/opt/ess2/prod/frontend" >> $GITHUB_OUTPUT
            echo "pm2_app=ess2-frontend-prod" >> $GITHUB_OUTPUT
            echo "port=3002" >> $GITHUB_OUTPUT
          else
            echo "deploy_path=/opt/ess2/uat/frontend" >> $GITHUB_OUTPUT
            echo "pm2_app=ess2-frontend-uat" >> $GITHUB_OUTPUT
            echo "port=3003" >> $GITHUB_OUTPUT
          fi

      - name: Copy build to Hetzner
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HETZNER_HOST }}
          username: ${{ secrets.HETZNER_USER }}
          key: ${{ secrets.HETZNER_SSH_KEY }}
          # If you don't use a next.config.js (or use .mjs/.ts instead), adjust this list
          source: ".next/,public/,package.json,package-lock.json,next.config.js"
          target: ${{ steps.vars.outputs.deploy_path }}

      - name: Install & restart on Hetzner
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HETZNER_HOST }}
          username: ${{ secrets.HETZNER_USER }}
          key: ${{ secrets.HETZNER_SSH_KEY }}
          script: |
            cd ${{ steps.vars.outputs.deploy_path }}
            npm install
            npm prune --omit=dev
            pm2 restart ${{ steps.vars.outputs.pm2_app }} --update-env \
              || pm2 start ./node_modules/.bin/next \
                  --name ${{ steps.vars.outputs.pm2_app }} \
                  -- start -p ${{ steps.vars.outputs.port }}
            pm2 save

      - name: Confirm deployment
        run: echo "✅ ESS2 Frontend deployed to ${{ github.event.inputs.environment }} on port ${{ steps.vars.outputs.port }}"
